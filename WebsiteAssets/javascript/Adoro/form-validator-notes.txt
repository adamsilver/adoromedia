1. Should we let users do

fv.addEventHandler("fieldName1.event", func)
fv.addEventHandler("fieldName2.event", func)

or should we allow a nice interface like

fv.addEventHandlerToAllValidators("event", func)

or something like

new Adoro.FormValidator(formNode {
	event: [func]	
})

2. Should Validator inner class be similar to Rule class?













